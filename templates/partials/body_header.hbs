<header>
    <h1>{{body_title}}</h1>

    <div>
        {{#if token}}
        <a href="/">Main</a>
        <a href="/home">Home</a>
        <a href="/tasks">Task</a>
        <form method='post' action='/logout'>
            <button>Logout</button>
        </form>
        {{else}}
        <a href="/login">Login</a>
        <a href="/register">Register</a>
        {{/if}}

    </div>
</header>